# GUI framework
PySide6>=6.5.0

# Windows-only hardware ID detection
WMI>=1.5.1; sys_platform == "win32"

# HTTP requests
requests>=2.28.0

# GPX file parsing
gpxpy>=1.5.0

# Critical: numpy must be <2.0 for compatibility with shapely 2.0.5
# numpy 2.x breaks shapely's create_collection ufunc
numpy>=1.23.0,<2.0

# Geometry processing
shapely>=2.0.5

# Map rendering
cartopy>=0.24.0
matplotlib>=3.10.0

# Image processing
Pillow>=10.0.0

# Video generation
moviepy>=1.0.3

# System process management
psutil>=5.9.0

# Optional: Progress bar for cache sweep (gracefully handled if missing)
tqdm>=4.65.0 